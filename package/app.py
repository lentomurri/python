from sys import path
import os

import modules as mod  # by importing modules inside the inity.py, we can avoid
# having to import the single modules from the package

print(mod.mod1.suml([i for i in range(0,5)]))




